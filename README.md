# Windows_desktop_switch

> EXE 放入启动文件夹（无需安装 AutoHotkey）
> 
## 1、打开启动文件夹
按 Win + R，输入 shell:startup，回车或手动导航到：

```cmd
C:\Users\<username>\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup
```

## 2、将`exe` 文件放到启动文件夹即可
